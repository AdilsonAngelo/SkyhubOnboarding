version: '3'

services:
  phoenix:
    build: phx_project
    ports:
      - 4000:4000
    depends_on:
      - mongo
    volumes:
      - ./phx_project:/app
  mongo:
    image: mongo:3.4
    restart: always
    environment:
      MONGO_INITDB_DATABASE: phx_project_dev
    #   MONGO_INITDB_ROOT_USERNAME: root
    #   MONGO_INITDB_ROOT_PASSWORD: rootpassword
    ports:
      - 27017:27017
    volumes:
      - ./mongo/data/db:/data/db